import { Body } from '../../app/factories/body';

describe('Body', () => {
  // const instance = () => {
  //   if (!this._instance) {
  //     this._instance = new Body();
  //   }
  //   return this._instance;
  // };
  // afterEach(() => this._instance = null);
  it('should ...', () => {
    expect(Body).toBeTruthy();
  });
});
