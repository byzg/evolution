// import * as _ from 'lodash';
//
// import { BaseCollection } from '../collections/base-collection';
// import { Body } from '../factories/body';
// import { Plant } from '../factories/bodies/alive/plant';
// import { Water } from '../factories/bodies/inanimate/water';
//
// let BodyClasses = [Water, Plant];
// export class BodyBuilder {
//   static readonly MAP = _.zipObject(_.map(BodyClasses, (BodyClass)=> BodyClass.name), BodyClasses);
//   static build(collectionName: string) {
//     // if (!BodyBuilder.MAP[bodyName]) throw `There is no body with name ${bodyName}`;
//     // return new BodyBuilder.MAP[bodyName](owner)
//   }
// }
